import "../styles/welcome.css";

export const Welcome = () => {
  return (
    <section className="welcome">
      <p>Cutting back on alcohol is hard. Cutback makes it easier.</p>
    </section>
  );
};
